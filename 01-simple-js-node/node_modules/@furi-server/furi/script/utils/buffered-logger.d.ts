import { LoggerMode } from '../types.js';
/**
 * Buffered logger class that uses a worker thread to handle logging.
 */
export declare class BufferedLogger {
    protected readonly logDirectory: string;
    protected readonly logFileName: string;
    protected enabled: boolean;
    protected terminal: boolean;
    protected flushPeriod: number;
    protected logMaxCount: number;
    protected logMode: LoggerMode;
    protected logLevel: string;
    private readonly worker;
    private readonly logLevelRank;
    private closed;
    constructor(logDirectory: string, logFileName: string, enabled: boolean, terminal: boolean, flushPeriod: number, logMaxCount: number, logMode: LoggerMode, logLevel: string);
    close(): void;
    debug(message: string | null): void;
    info(message: string | null): void;
    log(message: string | null): void;
    warm(message: string | null): void;
    error(message: string | null): void;
    critical(message: string | null): void;
    fatal(message: string | null): void;
}
//# sourceMappingURL=buffered-logger.d.ts.map